general:
    artifacts:
        - "build/com.zenika.docker.repository/com.zenika.docker.repository*.zip"

dependencies:
    post:
        - mvn clean install:
            pwd: com.zenika.docker.parent
