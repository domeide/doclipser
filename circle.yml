machine:
    environment:
       MAVEN_OPTS: "-Xmx512m -XX:MaxPermSize=128m"
    java:
       version: openjdk7

general:
    build_dir: com.zenika.docker.parent
    artifacts:
        - "com.zenika.docker.repository/target/com.zenika.docker.repository*.zip"

dependencies:
    post:
        - mvn clean install:
            pwd: com.zenika.docker.parent
